import { createMuiTheme } from '@material-ui/core/styles'

export const theme = {
  breakpoints: [640, 960]
}

export const muiTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#004dca'
    }
  }
})
