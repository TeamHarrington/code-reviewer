import { getUser, getUsers } from './user'
import { getAssignment, getAssignments } from './assignment'

export default {
  Query: {
    getUser,
    getUsers,
    getAssignment,
    getAssignments
  }
}
